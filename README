To do:
    - block, statements, statement, assignment, expression, controlStructureBody
    - Most of these are partially done
    - Once the grammar is complete, we can work on moving everything in main to somewhere else, possibly the auxillary section of k0gram.y?
Somewhat implemented:
    - statements, statement, expression - more to be done with these
Implemented:
    - topLevelObject, functionDeclaration, functionValueParameters, type, typeConstraints, functionBody
    - bison -dv -t debug options added to Makefile, to activiate, set int debug in main to 1. Shows exactly which token caused a syntax error.
    - expression, assignment, arithmetic
Notes:
    - 3 shift/reduce conflicts due to | LPAREN expr RPAREN rule in primary_expr and optional semicolons


Chris:
    semicolon insertion
    make all returns alctoken
    alctoken function
    modified: k0lex.l
Jaxen:
    semantic actions
    tree functionality
