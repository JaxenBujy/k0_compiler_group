Implemented:
    - added type.h, type.c
    - updated token, symbol table, and tree structs to include types
    - added basic type implementation to lookup() function
        - symbol table prints 100 for each ident for now
    
To-Do:
    - create type function that resolves types during symbol table creation

Teammates:
    Jaxen Bujold - 2
    Christopher Esquibel - 2