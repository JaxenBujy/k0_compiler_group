import something
import something_else
val x: Int = 5;
val y = 9;

// main function
fun main(args: Array<String>, arg_count: Int): Int {
    val z = 8;
    val g = 10;
    val message: String = "Hello, World!\n";
    println(message);
    for (jeg in (x+2)..z) {}
    
    if (x) y;
    else if (z) {}
    else a;
    g = 10 + (5 * 10) / 1;
    return x;
}