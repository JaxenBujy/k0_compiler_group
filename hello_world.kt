import something
import something_else
var x: Int = 5;
val y = 9;

// main function
fun main(args: Array<String>, arg_count: Int): Int {
    val z = 8;
    val g = 10;
    val message: String = "Hello, \tWorld!\n";
    println(message);
    for (jeg in x..100) {}
    
    if (x) y;
    else if (z) {}
    else a;
    g = 10 + (5 * 10) / 1;
    return x;
}